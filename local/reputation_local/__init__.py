"""Reputation — local program.

A per-user program that logs into Audible, lets the user search for books and
read reviews (saving each one locally), and share the collected reviews with the
central webserver.

The package is built from many small, single-responsibility ("dumb") classes.
See ../docs/LOCAL_PROGRAM.md for the design.
"""

__version__ = "0.1.0"
